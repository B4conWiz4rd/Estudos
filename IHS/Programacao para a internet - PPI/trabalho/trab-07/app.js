let diaSemana = prompt ('Qual o dia da semana? (Ex: Sábado)')

if (diaSemana == 'Sábado') {
    alert (`
        Hoje é ${diaSemana}, Bom fim de semana!
        `)
} else if (diaSemana == 'Domingo') {
    alert (`
        Hoje é ${diaSemana}, Bom fim de semana!
        `)
}
else {
    alert (`
        Hoje é ${diaSemana}, Boma semana!
        `)
}